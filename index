
<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<meta name="robots" content="noindex, nofollow, noarchive">
<title>Reference Guides</title>
<link href="https://fonts.googleapis.com/css2?family=Lora:ital,wght@0,400;0,500;0,600;0,700;1,400&family=Figtree:wght@300;400;500;600;700&family=Fira+Code:wght@400;500&display=swap" rel="stylesheet">
<style>
  *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

  :root {
    --cream: #f7f3ed;
    --cream-2: #ede8df;
    --cream-3: #e2ddd5;
    --charcoal: #2a2f2e;
    --charcoal-mid: #4a5250;
    --charcoal-light: #8a9896;
    --white: #ffffff;
    --grey-box: #f0f0f2;
    --grey-border: #dcdce0;
    --seafoam: #4caf8a;
    --seafoam-300: #70c9a8;
    --seafoam-500: #2d9e72;
    --seafoam-600: #1e845d;
    --seafoam-xl: #d0f0e3;
    --steel-500: #376690;
    --coral: #e8735a;

    --f-display: 'Lora', serif;
    --f-body: 'Figtree', sans-serif;
    --f-mono: 'Fira Code', monospace;
  }

  html { scroll-behavior: smooth; }

  body {
    font-family: var(--f-body);
    background: var(--white);
    color: var(--charcoal);
    line-height: 1.7;
    font-size: 15px;
    min-height: 100vh;
  }

  .top-accent {
    height: 4px;
    background: var(--seafoam-300);
  }

  .wrap {
    max-width: 760px;
    margin: 0 auto;
    padding: 80px 32px 96px;
  }

  /* ── HEADER ── */
  .badge {
    display: inline-block;
    font-family: var(--f-mono);
    font-size: 11px;
    color: var(--seafoam);
    background: var(--seafoam-xl);
    padding: 4px 10px;
    border-radius: 3px;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    margin-bottom: 18px;
  }
  h1 {
    font-family: var(--f-display);
    font-size: 42px;
    font-weight: 700;
    line-height: 1.1;
    margin-bottom: 16px;
  }
  .intro {
    font-size: 17px;
    color: var(--charcoal-mid);
    max-width: 600px;
  }

  .divider {
    border: none;
    border-top: 1px solid var(--cream-3);
    margin: 44px 0;
  }

  /* ── GUIDE LIST ── */
  .guides {
    display: flex;
    flex-direction: column;
    gap: 18px;
  }

  .guide-card {
    display: block;
    text-decoration: none;
    color: inherit;
    background: var(--grey-box);
    border: 1px solid var(--grey-border);
    border-left: 3px solid var(--seafoam-300);
    border-radius: 8px;
    padding: 24px 26px;
    transition: all 0.18s ease;
  }
  .guide-card:hover {
    border-left-color: var(--seafoam-500);
    background: var(--white);
    box-shadow: 0 6px 22px rgba(42,47,46,0.08);
    transform: translateY(-2px);
  }

  .guide-num {
    font-family: var(--f-mono);
    font-size: 12px;
    color: var(--seafoam-500);
    letter-spacing: 0.05em;
    margin-bottom: 8px;
  }
  .guide-title {
    font-family: var(--f-display);
    font-size: 22px;
    font-weight: 600;
    margin-bottom: 6px;
    display: flex;
    align-items: center;
    gap: 10px;
  }
  .guide-title .arrow {
    color: var(--seafoam-500);
    font-size: 18px;
    transition: transform 0.18s ease;
  }
  .guide-card:hover .guide-title .arrow { transform: translateX(4px); }
  .guide-desc {
    font-size: 14px;
    color: var(--charcoal-mid);
    max-width: 560px;
  }
  .guide-meta {
    font-family: var(--f-mono);
    font-size: 11px;
    color: var(--charcoal-light);
    text-transform: uppercase;
    letter-spacing: 0.08em;
    margin-top: 12px;
  }

  /* ── FOOTER ── */
  footer {
    margin-top: 56px;
    padding-top: 24px;
    border-top: 1px solid var(--cream-3);
    font-family: var(--f-mono);
    font-size: 11px;
    color: var(--charcoal-light);
    letter-spacing: 0.06em;
    text-transform: uppercase;
    display: flex;
    justify-content: space-between;
    gap: 12px;
    flex-wrap: wrap;
  }

  @media (max-width: 600px) {
    .wrap { padding: 56px 22px 72px; }
    h1 { font-size: 32px; }
    .intro { font-size: 16px; }
    .guide-card { padding: 20px 20px; }
    footer { flex-direction: column; }
  }
</style>
</head>
<body>

<div class="top-accent"></div>

<div class="wrap">

  <header>
    <span class="badge">Reference Guides</span>
    <h1>Reference Guides</h1>
    <p class="intro">A small collection of setup and style references — kept here for my own use and the occasional shared link.</p>
  </header>

  <hr class="divider">

  <main class="guides">

    <a class="guide-card" href="./ubuntu-setup.html">
      <div class="guide-num">01</div>
      <div class="guide-title">Ubuntu Setup Reference <span class="arrow">→</span></div>
      <p class="guide-desc">Commands, settings, and configurations for a privacy-focused Ubuntu 26.04 LTS setup — package management, firewall, encrypted DNS, security scanning, browsers, and maintenance.</p>
      <div class="guide-meta">8 sections · command reference</div>
    </a>

    <a class="guide-card" href="./website-setup.html">
      <div class="guide-num">02</div>
      <div class="guide-title">Building a Personal Website <span class="arrow">→</span></div>
      <p class="guide-desc">A step-by-step record of setting up a site on GitHub Pages — from an empty repository to a mobile-friendly, search-indexed site on a custom domain.</p>
      <div class="guide-meta">9 steps · how-to</div>
    </a>

    <a class="guide-card" href="./visual-brand-identity.html">
      <div class="guide-num">03</div>
      <div class="guide-title">Visual Brand Identity <span class="arrow">→</span></div>
      <p class="guide-desc">The design system behind these pages — color palette, typography, and the structural language used to keep everything visually consistent.</p>
      <div class="guide-meta">style guide · design tokens</div>
    </a>

  </main>

  <footer>
    <span>Reference Guides</span>
    <span>erikgartland.com</span>
  </footer>

</div>

</body>
</html>
